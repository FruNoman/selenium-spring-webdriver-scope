# Selenium Grid (RemoteWebDriverConfig). Point this at your hub, e.g.
# a docker-compose'd selenium/hub, or override with
# -Dgrid.url=... / GRID_URL env var without touching this file.
grid.url=http://localhost:4444/wd/hub
