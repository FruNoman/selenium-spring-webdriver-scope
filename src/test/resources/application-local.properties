# Local, in-process browser (WebDriverConfig). Nothing to configure here
# today, but this file exists so switching to `grid` below is just
# swapping which profile-specific file gets loaded, not adding one.
