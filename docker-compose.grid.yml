# A real Selenium Grid: a hub that routes sessions, and one node
# container per browser that actually runs it. Unlike the single
# selenium/standalone-chrome image used for a quick local check, this is
# the shape a CI pipeline or a Kubernetes deployment scales out — add
# more node services (or replicas of one) to add capacity.
services:
  selenium-hub:
    image: selenium/hub:4.49.0-20260909
    ports:
      - "4442:4442"
      - "4443:4443"
      - "4444:4444"
    # "healthy" = hub up AND nodes registered — `up -d --wait` waits on it.
    # Versions pinned to selenium-java in build.gradle (bump together);
    # tags up to 4.35 had no arm64 node-chrome, 4.49.0-20260909 is multi-arch.
    healthcheck:
      test: ["CMD", "/opt/bin/check-grid.sh", "--host", "0.0.0.0", "--port", "4444"]
      interval: 5s
      timeout: 10s
      retries: 24

  chrome:
    image: selenium/node-chrome:4.49.0-20260909
    shm_size: 2gb
    depends_on:
      - selenium-hub
    environment:
      - SE_EVENT_BUS_HOST=selenium-hub
      - SE_EVENT_BUS_PUBLISH_PORT=4442
      - SE_EVENT_BUS_SUBSCRIBE_PORT=4443
      # Raised for the parallel-Grid demo (10 concurrent TestNG threads);
      # one node process serving many sessions at once, not one node per
      # session — real capacity is still bounded by this container's CPU/RAM.
      - SE_NODE_MAX_SESSIONS=10
      - SE_NODE_OVERRIDE_MAX_SESSIONS=true

  firefox:
    image: selenium/node-firefox:4.49.0-20260909
    shm_size: 2gb
    depends_on:
      - selenium-hub
    environment:
      - SE_EVENT_BUS_HOST=selenium-hub
      - SE_EVENT_BUS_PUBLISH_PORT=4442
      - SE_EVENT_BUS_SUBSCRIBE_PORT=4443
